export const Checkbox = ({
    onChange,
    checked,
}: {
    onChange: () => void;
    checked: boolean;
}) => {
    return (
        <div>
            <h1>asd</h1>
        </div>
    );
};

export default Checkbox;
